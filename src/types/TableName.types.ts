/**
 * An enumeration of all the table names in the database.
 * These should be referenced in any SQL queries instead of hardcoding the table names.
 */
export enum TABLE_NAME {
  PRODUCTS = 'products',
  DISCOUNTS = 'discounts',
  RESTRICTIONS = 'restrictions',
  ORDERS = 'orders',
  ORDER_ITEM = 'order_items',
}
